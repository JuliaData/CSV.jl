# Delimited data

CSV.jl reads and writes tables stored as delimited text.

## Language

**Source column**: A column found in the input.

**Column request**: A user's choice of source columns and declared result types.

**Result column**: A column returned to the caller.
